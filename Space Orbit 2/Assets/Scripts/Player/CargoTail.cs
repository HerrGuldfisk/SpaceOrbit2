using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CargoTail : LimbsBehaviour2
{
    // Start is called before the first frame update
    void Start()
    {
        segmentCount = 1;
        InitLimb();
    }
}
